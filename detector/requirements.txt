# Runtime dependencies only. Training deps are intentionally excluded (see
# requirements-train.txt): torch is ~1 GB and the serving image has no use for it.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
numpy==2.2.1
opencv-python-headless==4.10.0.84
onnxruntime==1.20.1
